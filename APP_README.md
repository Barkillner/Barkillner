# 🎯 מנהל התוכן האסטרטגי — אייל נווה

אפליקציית Streamlit מקומית שמנהלת את המותג של אייל נווה ומעבירה אותו מ"איש מחאה"
ל"מנהיג אזרחי יוזם". כל ממשק המשתמש והתוכן הנוצר — בעברית.

## מה זה עושה

- **סריקה חכמה (Smart Scanning)** — שני שלבים:
  1. **גירוד רחב** — `feedparser` מושך כותרות מפידי RSS ישראליים (ynet, וואלה, הארץ, מעריב).
  2. **סינון הקשרי (המוח)** — Claude מסנן רק את הכותרות הרלוונטיות אסטרטגית לפי
     האסטרטגיה של אייל, גם ללא מילות מפתח.
- **דשבורד חי** — כרטיסים אסטרטגיים עם כפתור "נתח אסטרטגית" שפותח ניתוח מלא:
  ניתוח הידיעה, הקשר אסטרטגי, נרטיב וזווית, חבילת תוכן (פייסבוק / אינסטגרם /
  סטורי / טיקטוק-ריל) והוראות הפקה (צילום ותבנית ויזואלית).
- **מתכנן שבועי** — הזנת כיוונים שבועיים ⟵ תוכנית תוכן מובנית בטבלה (יום, פילר,
  פורמט, טיוטה) הניתנת להורדה כ-CSV.

האסטרטגיה של אייל (שלושת הצירים, ארבעת הפילרים, טבלת האסוציאציות וכללי הקול)
מוטמעת ב-`strategy.py` ומזרימה כל סינון, ניתוח ותכנון.

## מבנה הפרויקט

| קובץ | תפקיד |
|---|---|
| `app.py` | ממשק Streamlit (RTL, שלוש תצוגות) |
| `strategy.py` | תמצית האסטרטגיה וכללי הקול המוזרקים ל-Claude |
| `news.py` | גירוד כותרות מפידי RSS (`feedparser`) |
| `ai.py` | עטיפות ל-Claude API: סינון, ניתוח, תכנון שבועי |
| `requirements.txt` | תלויות |

## התקנה והרצה

```bash
# מומלץ בסביבה וירטואלית (פותר בעיית build של feedparser בהפצות דביאן)
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt

streamlit run app.py
```

הדפדפן ייפתח על `http://localhost:8501`.

> **הערה על feedparser:** בחלק ממערכות דביאן/אובונטו, `pip install feedparser`
> נכשל בבניית התלות `sgmllib3k` מול ה-setuptools המערכתי. התקנה בתוך `venv`
> נקי (כמו למעלה) פותרת זאת.

## שימוש

1. בסרגל הצד (**ניהול והגדרות**) הזן **מפתח Anthropic API**.
2. אפשר לערוך את **פידי ה-RSS** ולבחור **מודל** (Sonnet 5 כברירת מחדל).
3. **דשבורד חי** ⟵ "סרוק חדשות עכשיו" ⟵ לחץ "נתח אסטרטגית" על כרטיס.
4. **תוכן שבועי** ⟵ הזן כיוונים ⟵ "צור תוכנית שבועית".

מפתח ה-API מוזן בזמן ריצה ואינו נשמר לקובץ.

> כל התוכן הנוצר הוא **טיוטה לאישור אייל**.

## פרסום חי — Streamlit Community Cloud

כדי לקבל כתובת אינטרנט חיה שעובדת באמת (סורקת RSS וקוראת ל-Claude), פורסים את
האפליקציה ל-[Streamlit Community Cloud](https://share.streamlit.io) — חינמי ומתחבר
ישירות לריפו ב-GitHub.

1. היכנס ל-<https://share.streamlit.io> והתחבר עם חשבון ה-GitHub.
2. **Create app** ⟵ בחר את הריפו `Barkillner/Barkillner`, את הענף (למשל `main`), וקובץ
   ראשי `app.py`.
3. **Advanced settings** ⟵ בחר **Python 3.11** (או חדש יותר).
4. **Deploy** — הפריסה מתקינה את `requirements.txt` ומעלה את האפליקציה תוך דקה-שתיים.

לאחר הפריסה מקבלים כתובת ציבורית (כמו `https://<app>.streamlit.app`). כל משתמש
מדביק את **מפתח ה-Anthropic שלו** בסרגל הצד — אין צורך לשמור סוד בשרת.

### להטמעת מפתח כסוד (אופציונלי)

אם רוצים שהאפליקציה תעבוד בלי שאף אחד ידביק מפתח, מגדירים סוד ב-Streamlit Cloud
(**Settings ⟵ Secrets**):

```toml
ANTHROPIC_API_KEY = "sk-ant-..."
```

הקוד כבר קורא את הסוד הזה אוטומטית (`app.py` ⟵ `_secret_api_key()`): אם הוא מוגדר,
שדה המפתח בסרגל הצד מאותחל ממנו והאפליקציה עובדת מיד; אם לא — נופלים בבטחה למצב שבו
המשתמש מדביק מפתח ידנית. שים לב: כשמטמיעים סוד, כל מי שנכנס לכתובת משתמש במפתח שלך
ובחיוב שלך — עדיף רק לאפליקציה פרטית.

> **תזכורת:** ה-Artifact שנוצר בצ'אט הוא **הדגמה ויזואלית בלבד** (ארגז חול ללא גישה
> לרשת). הגרסה החיה שמפורסמת כאן היא אפליקציית ה-Streamlit הזו.
